"use strict"
/**
 * ENIGME
 * Faire une fonction qui compte jusqu'à zéro depuis un nombre
 * de départ donné (Exemple si je donne 3 : 3, 2, 1, 0)
 *
 * Le problème ? INTERDICTION D'UTILISER LES BOUCLES !
 *
 * @param {number} number
 * @return {number}
 */
function countdown(number) {
    return 0;
}

/**
 * Suite de fibonacci
 *
 * @param {number} n
 * @returns {number}
 */
function fibonacci(n) {
    return 0;
}
